import 'package:flutter/material.dart';

class PrimariaAsistenciaTab extends StatelessWidget {
  const PrimariaAsistenciaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Asistencia (pendiente)'));
  }
}
