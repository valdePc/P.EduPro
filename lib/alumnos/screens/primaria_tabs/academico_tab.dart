import 'package:flutter/material.dart';

class PrimariaAcademicoTab extends StatelessWidget {
  const PrimariaAcademicoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Académico (pendiente)'));
  }
}
